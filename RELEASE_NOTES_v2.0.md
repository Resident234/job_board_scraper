# Release Notes - JobBoardScraper v2.0.0

**Дата релиза:** 4 ноября 2024  
**Тип релиза:** Major Update

---

## 🎉 Основные изменения

### 1. ExpertsScraper - Новый скрапер для экспертов

Добавлен полнофункциональный скрапер для сбора данных экспертов с career.habr.com:

- ✅ Автоматический обход всех страниц экспертов
- ✅ Извлечение расширенных данных: имя, код, стаж работы, компания
- ✅ Интеграция с существующей системой логирования и БД
- ✅ Настраиваемый интервал обхода (по умолчанию: 4 дня)
- ✅ Поддержка режима `UpdateIfExists` для обновления данных

**Пример использования:**
```xml
<add key="Experts:Enabled" value="true" />
<add key="Experts:OutputMode" value="Both" />
```

---

### 2. SmartHttpClient - Универсальная обёртка

Создана единая обёртка над HttpClient, объединяющая функциональность retry и измерения трафика:

- ✅ Автоматические повторы с экспоненциальной задержкой
- ✅ Измерение HTTP-трафика для каждого скрапера
- ✅ Настраиваемый timeout для каждого скрапера
- ✅ Поддержка заголовка `Retry-After`
- ✅ Обработка транзиентных ошибок (408, 429, 500, 502, 503, 504)

**Преимущества:**
- Устранено дублирование кода
- Единообразный подход к HTTP-запросам
- Лучшая обработка ошибок

---

### 3. Система измерения HTTP-трафика

Добавлена полноценная система мониторинга трафика:

- ✅ Автоматический подсчёт размера каждого HTTP-ответа
- ✅ Статистика по каждому скраперу отдельно
- ✅ Общая статистика по всем скраперам
- ✅ Сохранение в файл с настраиваемым интервалом
- ✅ Форматированный вывод с человекочитаемыми размерами

**Пример статистики:**
```
OVERALL STATISTICS:
  Total Requests: 1,234
  Total Traffic:  45.67 MB
  Average/Request: 37.92 KB

PER-SCRAPER STATISTICS:
  ExpertsScraper:
    Requests:     234
    Total:        8.2 MB
    Avg/Request:  35.04 KB
```

---

### 4. Независимое логирование

Полностью переработана система логирования:

- ✅ Отдельные лог-файлы для каждого скрапера
- ✅ Три режима вывода: `ConsoleOnly`, `FileOnly`, `Both`
- ✅ Логи больше не смешиваются между скраперами
- ✅ Формат имени файла: `{ScraperName}_{timestamp}.log`

**Настройка:**
```xml
<add key="Experts:OutputMode" value="Both" />
<add key="Companies:OutputMode" value="FileOnly" />
```

---

### 5. Управление скраперами через конфигурацию

Каждый скрапер теперь можно включить или отключить:

- ✅ Настройка `{Scraper}:Enabled` для каждого скрапера
- ✅ Вывод статуса при запуске приложения
- ✅ Гибкая конфигурация под разные сценарии

**Пример:**
```xml
<add key="Experts:Enabled" value="true" />
<add key="CompanyFollowers:Enabled" value="false" />
<add key="BruteForce:Enabled" value="false" />
```

**Вывод при запуске:**
```
[Program] ExpertsScraper: ВКЛЮЧЕН
[Program] CompanyFollowersScraper: ОТКЛЮЧЕН
[Program] BruteForceUsernameScraper: ОТКЛЮЧЕН
```

---

### 6. Расширение структуры базы данных

Добавлены новые столбцы в таблицу `habr_resumes`:

- ✅ `code` (TEXT) - код пользователя из URL
- ✅ `expert` (BOOLEAN) - флаг эксперта
- ✅ `work_experience` (TEXT) - стаж работы
- ✅ Индексы для быстрого поиска

**SQL-скрипт:** `sql/add_expert_columns.sql`

---

## 📊 Статистика изменений

### Новые файлы (10)
1. `JobBoardScraper/Helper.ConsoleHelper/ConsoleLogger.cs`
2. `JobBoardScraper/Helper.ConsoleHelper/OutputMode.cs`
3. `JobBoardScraper/SmartHttpClient.cs`
4. `JobBoardScraper/TrafficStatistics.cs`
5. `JobBoardScraper/WebScraper/ExpertsScraper.cs`
6. `sql/add_expert_columns.sql`
7. `MIGRATION_GUIDE.md`
8. `QUICKSTART.md`
9. `CHANGELOG.md`
10. `EXAMPLES.md`

### Удалённые файлы (3)
1. `JobBoardScraper/HttpRetry.cs` → заменён на `SmartHttpClient`
2. `JobBoardScraper/TrafficMeasuringHttpClient.cs` → заменён на `SmartHttpClient`
3. `JobBoardScraper/Helper.ConsoleHelper/DualWriter.cs` → заменён на `ConsoleLogger`

### Изменённые файлы (11)
- `Program.cs`, `DatabaseClient.cs`, `AppConfig.cs`, `App.config`
- Все скраперы переведены на новую архитектуру
- Обновлена документация

---

## 🚀 Миграция с v1.x

### Шаг 1: Обновление базы данных

```bash
psql -U postgres -d jobs -f sql/add_expert_columns.sql
```

### Шаг 2: Обновление конфигурации

Добавьте в `App.config`:

```xml
<!-- ExpertsScraper Settings -->
<add key="Experts:Enabled" value="true" />
<add key="Experts:ListUrl" value="https://career.habr.com/experts?order=lastActive" />
<add key="Experts:EnableTrafficMeasuring" value="true" />
<add key="Experts:OutputMode" value="Both" />

<!-- Traffic Statistics Settings -->
<add key="Traffic:OutputFile" value="./logs/traffic_stats.txt" />
<add key="Traffic:SaveIntervalMinutes" value="5" />
```

### Шаг 3: Пересборка

```bash
dotnet build JobBoardScraper/JobBoardScraper.csproj -c Release
```

**Подробнее:** См. [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)

---

## 📚 Документация

### Новые руководства
- **[QUICKSTART.md](QUICKSTART.md)** - быстрый старт для новых пользователей
- **[MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)** - руководство по миграции с v1.x
- **[EXAMPLES.md](EXAMPLES.md)** - практические примеры использования
- **[CHANGELOG.md](CHANGELOG.md)** - полная история изменений

### Обновлённая документация
- **[README.md](JobBoardScraper/README.md)** - основная документация
- **[sql/README.md](sql/README.md)** - работа с базой данных
- **[docs/TRAFFIC_OPTIMIZATION.md](docs/TRAFFIC_OPTIMIZATION.md)** - оптимизация трафика

---

## 🎯 Решённые проблемы

1. ✅ **Смешанные логи** - каждый скрапер теперь имеет свой лог-файл
2. ✅ **Дублирование кода** - создан универсальный `SmartHttpClient`
3. ✅ **Отсутствие контроля трафика** - добавлена система мониторинга
4. ✅ **Невозможность отключить скраперы** - добавлены настройки `{Scraper}:Enabled`
5. ✅ **Проблемы с кодировкой** - правильное определение из заголовков
6. ✅ **Отсутствие данных о экспертах** - добавлен `ExpertsScraper`
7. ✅ **Ограниченная структура БД** - добавлены новые столбцы

---

## 🔧 Технические улучшения

### Архитектура
- Удалено дублирование кода HTTP-запросов
- Улучшена обработка ошибок
- Единообразный подход к логированию
- Лучшая модульность и расширяемость

### Производительность
- Оптимизирована работа с HTTP-клиентами
- Улучшена обработка больших объёмов данных
- Эффективное использование памяти

### Надёжность
- Автоматические повторы при ошибках
- Корректная обработка транзиентных ошибок
- Graceful shutdown всех процессов

---

## 📈 Примеры использования

### Сценарий 1: Только эксперты
```xml
<add key="Experts:Enabled" value="true" />
```
**Результат:** Сбор экспертов каждые 4 дня, ~5-10 MB трафика

### Сценарий 2: Эксперты + Компании
```xml
<add key="Experts:Enabled" value="true" />
<add key="Companies:Enabled" value="true" />
<add key="Category:Enabled" value="true" />
```
**Результат:** Полный сбор данных, ~50-100 MB трафика

### Сценарий 3: Полный набор
```xml
<add key="Experts:Enabled" value="true" />
<add key="CompanyFollowers:Enabled" value="true" />
<add key="Companies:Enabled" value="true" />
<add key="Category:Enabled" value="true" />
<add key="ResumeList:Enabled" value="true" />
```
**Результат:** Максимальный охват, ~500 MB - 1 GB в неделю

**Подробнее:** См. [EXAMPLES.md](EXAMPLES.md)

---

## 🔍 SQL-запросы для экспертов

### Получить всех экспертов
```sql
SELECT * FROM habr_resumes WHERE expert = TRUE ORDER BY title;
```

### Топ-10 по стажу
```sql
SELECT title, work_experience, link 
FROM habr_resumes 
WHERE expert = TRUE AND work_experience IS NOT NULL
ORDER BY CAST(SUBSTRING(work_experience FROM '(\d+)') AS INTEGER) DESC
LIMIT 10;
```

### Статистика
```sql
SELECT 
    COUNT(*) AS total_experts,
    COUNT(work_experience) AS with_experience,
    COUNT(slogan) AS with_slogan
FROM habr_resumes 
WHERE expert = TRUE;
```

**Подробнее:** См. [EXAMPLES.md](EXAMPLES.md#sql-запросы)

---

## ⚠️ Breaking Changes

### Удалённые классы
- `HttpRetry` → используйте `SmartHttpClient`
- `TrafficMeasuringHttpClient` → используйте `SmartHttpClient`
- `DualWriter` → используйте `ConsoleLogger`

### Изменения в API
- `DatabaseClient.EnqueueResume()` теперь принимает дополнительные параметры:
  - `code` (string?)
  - `expert` (bool?)
  - `workExperience` (string?)

### Изменения в конфигурации
- Добавлены обязательные настройки для `ExpertsScraper`
- Добавлены настройки статистики трафика

---

## 🐛 Известные ограничения

1. **Интервал обхода** задаётся в коде, а не в конфигурации
2. **BruteForce** может создавать высокую нагрузку на сервер
3. **Логи** могут занимать много места при режиме `Both`

---

## 🔮 Планы на будущее

### v2.1 (планируется)
- [ ] Настройка интервалов обхода через `App.config`
- [ ] Автоматическая ротация лог-файлов
- [ ] Web-интерфейс для мониторинга
- [ ] Экспорт статистики в JSON/CSV

### v2.2 (планируется)
- [ ] Поддержка прокси-серверов
- [ ] Распределённый скрапинг
- [ ] API для внешних приложений

---

## 💬 Обратная связь

Если у вас есть вопросы, предложения или вы нашли баг:

1. Проверьте [EXAMPLES.md](EXAMPLES.md) и [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)
2. Изучите логи в директории `./logs/`
3. Проверьте статистику трафика в `./logs/traffic_stats.txt`

---

## 🙏 Благодарности

Спасибо всем, кто использует JobBoardScraper и предоставляет обратную связь!

---

**Версия:** 2.0.0  
**Дата:** 4 ноября 2024  
**Статус:** Stable ✨
